= simple_form_for [@ad_campaign, @ad] do |f|
  = render 'shared/error_box', model: @ad
  .row
    .small-6.columns
      = f.input :file
    .small-6.columns
      = f.association :ad_size, label_method: :format_for_select
  .row
    .small-6.columns
      = f.input :href
    .small-6.columns
      - if @ad.file?
        = image_tag @ad.file.url
  .row
    .column
      = f.button :submit, t('.submit')
